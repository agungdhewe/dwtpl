package dwtpl

import (
	"text/template"
)

func (mgr *TemplateManager) ParsePageTemplate(pagename string, dir string) (map[DeviceType]*template.Template, bool, error) {
	// ambil data layout

	return nil, true, nil
}
